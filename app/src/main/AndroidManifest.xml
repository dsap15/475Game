<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a475game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.475Game">
        <activity
            android:name=".BackButtonTest"
            android:exported="false"
            android:label="@string/title_activity_back_button_test"
            android:theme="@style/Theme.475Game.NoActionBar" />
        <activity
            android:name=".LevelEditor"
            android:exported="false" />
        <activity android:name=".start_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>